{
  "name": "backend",
  "version": "0.0.1",
  "description": "chat app",
  "main": "index.ts",
  "scripts": {
    "start:dev": "npx nodemon ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js",
    "client": "npx nodemon ./src/client.ts"
  },
  "author": "Giorgi Gvimradze",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node14": "^1.0.1",
    "@types/express": "^4.17.13",
    "@types/node": "^14.18.20",
    "nodemon": "^2.0.16",
    "rimraf": "^3.0.2",
    "ts-node": "^10.8.0",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "express": "^4.18.1",
    "socket.io": "^4.5.1",
    "socket.io-client": "^4.5.1"
  }
}
